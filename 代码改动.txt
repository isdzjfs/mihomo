2026-05-21

- 修正 proxy-group 的 health-check 候选过滤：`include-all`/`use` 场景下注册自动延迟测试时，现在会同步应用 `exclude-filter` 与 `exclude-type`，避免已被组排除的节点仍被后台测速。
- 抽取组内排除逻辑，确保 `GroupBase.GetProxies()` 与 compatible provider 的 health-check 候选使用同一套排除判断。
- provider 额外 health-check 对相同 URL 的多组注册按“任务候选并集”执行，避免某个组的 `exclude-type` 误伤另一个同 URL 组的测速候选。
- 新增针对性测试，覆盖 provider 额外 health-check 任务的名称/类型排除，以及 `include-all` 生成 compatible provider 前的排除过滤。
- 验证：`rtk go test ./adapter/provider ./adapter/outboundgroup` 通过。

2026-05-25

- 调整过期 provider 的启动强制刷新时机：`forceUpdate` 不再在 `pullLoop()` 开头立即执行 `updateWithLog()`，而是先等待一次最小 backoff，让外层 config apply、选组回填和 inner tunnel 状态先落稳。
- 强制刷新日志移动到实际刷新前打印，并只在延后的首次刷新时触发；正常 interval 与失败 backoff 的重试语义保持不变。
- 验证：`rtk gofmt -w component/resource/fetcher.go`；`rtk go test ./component/resource` 通过（该包当前无测试文件）。

2026-05-27

- 修正 `url-test` 自动选择在节点存活状态异步刷新时可能保留失效当前节点的问题：`fast()` 现在在同一轮扫描内固定当前节点的 alive/delay 快照，避免扫描阶段已判定 not alive 的节点因后续重复读取状态翻转而继续被选中。
- 自动模式下 `DialContext` / `ListenPacketContext` 拨号失败时立即重置 `fastSingle` 缓存，避免后续连接在 health-check 触发期间继续复用已失败的旧选择。
- 新增回归测试，覆盖当前 `fastNode` 在扫描时不可用、后续 alive 状态翻转时仍应切换到其他可用节点的场景。
- 验证：`rtk gofmt -w adapter/outboundgroup/urltest.go adapter/outboundgroup/parser_test.go adapter/outboundgroup/urltest_test.go`；`rtk go test ./adapter/outboundgroup` 通过；`rtk go test ./adapter/...` 通过。

2026-06-03

- 修复配置热更新时旧 provider / rule-provider / proxy 未关闭导致的资源泄漏：`tunnel.UpdateProxies()` / `tunnel.UpdateRules()` 现在返回被替换前的对象快照，`ApplyConfig()` 在新 provider 初始化后关闭旧对象，避免 reload 后 goroutine、health-check 与 inbound 资源残留。
- 补齐旧 proxy adapter 本体的关闭链路：`ApplyConfig()` 在新配置加载完成、旧 provider 停止后显式关闭被替换的旧 proxy，`baseProvider.Close()` 也会关闭自身持有的 provider proxies，避免 Hysteria、Mieru、WireGuard、OpenVPN、SSH、mux client 等可能持有长期资源的 adapter 在 reload 后继续存活。
- 加固 tunnel 与 HealthCheck 的并发访问：`proxies` / `providers` / `ruleProviders` 对外返回快照，`mode` 改为 atomic enum；HealthCheck 更新代理列表和执行测速时使用快照，避免配置 reload 与测速任务并发读写。
- 修正非法 regex 会 panic 的问题：proxy-group 的 `filter` / `exclude-filter` 从 `MustCompile` 改为可返回错误的 `Compile`，解析阶段向调用方返回配置错误，并新增无效 regex 回归测试。
- 修复 httpmask poll pull 分支的 `cancel()` 泄漏：decode 失败和连接关闭退出前都会释放请求 context，避免长轮询异常路径残留资源。
- 修复 Mieru inbound 重复 `Listen()` 可能启动多个 accept loop 的崩溃风险：增加 `accepting` 状态保护，并取消 Mieru 四组子用例并行执行，避免测试中的外层并行触发 fatal crash。
- 验证：`rtk go test ./...` 通过。

2026-06-04

- 修复 `BUG-T6-01`：MASQUE H2 DATAGRAM capsule 在按远端声明长度分配前增加 65535 字节上限，超限直接返回协议错误，避免恶意 MASQUE peer 触发超大内存分配。
- 修复 `BUG-T6-02`：Trojan UDP `WaitReadFrom()` 在切片和读取 payload 前校验长度不超过协议分片上限与实际池缓冲长度，避免畸形 UDP 包通过长度字段触发 panic。
- 修复 `BUG-F6-02`：AnyTLS padding scheme 增加 raw scheme、stop、单条 record payload、每包规则数量上限；远端 `cmdUpdatePaddingScheme` 和写路径都加兜底检查，避免服务端下发超大 padding 方案造成内存/带宽放大。
- 修复 `BUG-F6-03`：VLESS Vision ServerHello 解析改为按 TLS record / handshake / session ID 边界逐段校验，只在字段完整且长度合法时读取 cipher suite，避免短包或畸形 ServerHello 触发越界 panic。
- 修复 `BUG-A7-04`：SOCKS4 NUL 结尾字段增加 255 字节上限，超过后返回握手错误，避免未发送 NUL 的客户端无限增长内存占用。
- 新增回归测试覆盖 MASQUE oversized capsule、Trojan oversized UDP length、AnyTLS oversized padding scheme/range、VLESS Vision malformed ServerHello、SOCKS4 oversized NUL field。
- 本次按要求未处理 `BUG-A7-01`（Unix socket / Windows named pipe external controller secret bypass）。
- 验证：`rtk go test ./transport/masque ./transport/trojan ./transport/anytls/... ./transport/vless/vision ./transport/socks4` 通过；`rtk go test ./listener/anytls ./listener/inbound ./adapter/outbound` 通过；`rtk git -c safe.directory=C:/Users/Sen/Documents/GitHub/mihomo diff --check` 通过。

2026-06-04（P1 补充）

- 修复 reload/listener 非原子替换问题：HTTP、SOCKS、Mixed、Redir、TProxy、ShadowSocks、Vmess、Tuic 和 custom inbound 现在先构造/启动新实例，成功后再替换并关闭旧实例；失败时保留旧监听，避免坏 reload 直接中断原服务。
- 修复 Redir/TProxy UDP 同配置 reload 不重试问题：TCP 与 UDP 监听状态分开判断，TCP 未变但 UDP 缺失或上次启动失败时仍会重试 UDP 侧。
- 修复 DNS reload 非原子和异步启动不可见问题：DNS TCP/UDP socket 先同步绑定成功后再发布新 `Server`，失败时保留旧 DNS server；服务对象内部读取也加锁保护。
- 修复 Linux iptables 半应用问题：`SetTProxyIPTables()` 现在逐步检查命令错误，失败后调用现有 cleanup 回滚已应用的 route/rule/chain；cleanup 也会在部分 chain 尚未创建时继续尝试基础清理。
- 修复 rule matching 与 provider 更新竞态：rule-provider strategy 改为锁保护快照访问；`tunnel.match()` 只在全局配置锁内复制 rules/proxies 快照，锁外执行慢规则匹配、DNS 解析和进程查询，避免 traffic path 长时间持有配置锁。
- 修复 Fetcher 与 provider close 生命周期竞态：`hash` / `updatedAt` 统一受互斥锁保护，`Close()` 会取消后台任务、关闭 watcher 并等待正在执行的 update/callback 结束，避免旧 provider 在 reload 后继续发布结果。
- 修复 sniffer、resolver/hosts、入站 auth/IP filter 的 reload 并发读写：sniffer runtime state 改为 atomic snapshot；resolver、host mapper、hosts、DNS service、IPv6 开关和 system hosts 开关通过 getter/setter 加锁发布；入站白名单/跳过认证前缀改为不可变快照。
- 补齐 adapter/pool 关闭链路：TUIC `PoolClient` 增加整体 `Close()` 并关闭 TCP/UDP client；Snell reuse pool 增加 pool-wide close；TrustTunnel client/pool 使用可取消 context，并在 close 时等待 health-check goroutine 退出。
- 修复协议与资源预算类 P1：AnyTLS data frame 按 16-bit 上限分片并限制 session stream 数；Sudoku multiplex 增加 session stream 数、accept 队列、stream queued bytes/frames 上限；VLESS addon LEN 字段、ShadowSocks AEAD 小缓冲、MRS/domain/CIDR 二进制长度字段均增加分配前校验。
- 修复 focused protocol 多地址 listener 部分构造失败泄漏：AnyTLS、Trojan、Sing Shadowsocks 在任一地址启动失败时会关闭已创建的 listener，并将 `closed` 标志改为 atomic，避免 accept loop 与 close 并发读写。
- 本次按要求跳过 `BUG-R1-01`、`BUG-R1-02`、`BUG-R1-07`、`BUG-R1-08`、`BUG-R1-09`、`BUG-A7-07`、`BUG-A7-08`，并跳过控制面安全相关的 A7 项。
- 验证：`rtk gofmt -w ...` 已执行；`rtk go test ./...` 通过，174 个包共 4416 个测试通过。

2026-06-04（BUG-R1-02 补充）

- 修复 `BUG-R1-02`：`hub/executor.ApplyConfig()` 现在返回聚合错误，DNS、主监听端口、custom inbound、TUN、tunnels、iptables、proxy/rule provider 初始化失败都会汇总给调用方。
- 配置 REST reload 已接入错误返回：`PUT /configs` 在 apply 失败时返回 500，不再无条件 204；`PATCH /configs` 的端口/TUN/TUIC 等热改失败也会返回 500。
- 修复依赖 R1-02 才能完整暴露的 reload 问题：DNS 重建失败、TUN 创建失败、Redir/TProxy UDP 半边失败、动态 tunnel 创建失败、iptables 配置失败现在都会保留可保留的旧实例并返回错误。
- 补充 `hub.Parse()` 顶层错误传播：启动阶段 apply 失败会向调用方返回错误；外部控制器 `BUG-R1-09` 仍按先前要求保持跳过。
- 验证：`rtk gofmt -w dns\server.go listener\listener.go hub\executor\executor.go hub\route\configs.go hub\hub.go` 已执行；`rtk go test ./...` 通过，174 个包共 4416 个测试通过。

2026-06-04（BUG-R1-14 补充）

- 完整修复 `BUG-R1-14` 文档点名的 WireGuard / Masque adapter 生命周期缺口：构造函数增加 ownership commit 前的 rollback defer，失败发生在 stack device、wireguard-go device、resolver 或 H2 transport 创建后时会调用 adapter `Close()` 统一清理。
- WireGuard `Close()` 改为 nil-safe、idempotent，并区分已构造 wireguard-go device 与部分构造失败路径；正常路径交给 device 关闭 TUN/bind，部分构造路径手动关闭已创建的 TUN/bind。
- Masque `Close()` 改为 nil-safe、idempotent，关闭 run context、H2 idle connections 和 TUN device，避免 H2 transport 或本地 device 在 adapter 替换后继续持有资源。
- 补充同类生命周期 audit 中发现的 Shadowsocks/kcptun 漏点：`UDPOverTCPVersion` 校验提前到 kcptun client 创建之前，避免非法 UOT 配置在构造失败时留下 kcptun client。
- 新增 WireGuard / Masque 构造失败回滚测试，覆盖 stack device 已创建后 DNS 解析失败时 device 必须恰好关闭一次。
- 验证：`rtk gofmt -w adapter\outbound\wireguard.go adapter\outbound\masque.go adapter\outbound\shadowsocks.go adapter\outbound\lifecycle_test.go` 已执行；`rtk go test ./adapter/outbound` 通过，4 个测试通过；`rtk go test ./...` 通过，174 个包共 4418 个测试通过；`rtk git -c safe.directory=C:/Users/Sen/Documents/GitHub/mihomo diff --check` 通过。

2026-06-04（P2 补充）

- 修复 `BUG-R1-10`：geo updater 改为受管理的运行时组件，`geo-auto-update` 和 `geo-update-interval` reload 时会通过同一入口重配；关闭自动更新会取消旧 ticker，调整 interval 会重启后台更新循环。
- 修复 `BUG-R1-11` / `BUG-R1-12` / `BUG-R1-13`：TUN same-config reload 的 routing mark/interface finder 恢复失败现在会返回错误；custom inbound 重名检测改为检查最终 listener map；`allow-lan` / `bind-address`、ETag、ShadowSocks 默认 listener 等全局状态改为 atomic snapshot/pointer 发布。
- 修复统计、provider、selector 与 fake-IP 的 P2 竞态：连接统计 `memory` 改为 atomic；provider `subscriptionInfo` 读写纳入 provider mutex；proxy provider override 后按最终 `proxy.Name()` 去重并报错；selector 手动选择加锁；fake-IP clone/flush/store-state 与分配路径共用同一把锁。
- 修复协议边界类 P2：v2ray-plugin mux 拒绝超过 65535 字节的单帧写入；TUIC v5 修正 `BytesLen()` 与分片 `ReadFrom()` 重组数据来源；TUIC v4/v5 `MaxOpenStreams` 改为超过上限才拒绝；Sudoku UoT/KIP 最大 payload 改为 `0xffff`；VLESS 域名长度和 UDP packet 长度写入前校验。
- 修复 Hysteria / Hysteria2 / KCP 生命周期与校验问题：fakeTCP cleaner 改为持续 ticker 循环并停止 ticker；UDP hop 使用 server address map 校验包来源；KCP/smux 构造失败路径补齐底层 packet/KCP 连接回滚；Hysteria2 Realm listener 保存并关闭 HTTP server，reaper 在 context 取消时直接退出；Hysteria2 IPv6-only resolver 改为 IPv6 查询。
- 修复 ShadowSocks UDP 包装器固定 64 KiB scratch buffer 问题：AEAD/stream packet 写路径按实际加密后长度申请 buffer，并保留最大包大小限制，避免小 UDP 包高并发时占用过大的临时缓冲。
- 验证：`rtk gofmt -w ...` 已执行；`rtk go test ./transport/v2ray-plugin ./transport/tuic/v4 ./transport/tuic/v5 ./transport/sudoku ./transport/vless ./listener/sing_vless ./adapter/outbound ./transport/kcptun ./transport/hysteria/conns/faketcp ./transport/hysteria/conns/udp ./listener/hysteria2_realm ./tunnel/statistic ./component/resource ./component/fakeip ./adapter/outboundgroup ./config ./adapter/provider ./listener ./listener/sing_tun ./component/updater ./hub/executor ./listener/sing_shadowsocks ./listener/shadowsocks ./transport/shadowsocks/shadowaead ./transport/shadowsocks/shadowstream` 通过，25 个包共 82 个测试通过；`rtk git -c safe.directory=C:/Users/Sen/Documents/GitHub/mihomo diff --check` 通过。

2026-06-05（P3 补充）

- 按要求跳过 `BUG-F6-12`，未处理 Hysteria2 `max-idle-time` 配置被接受但未应用的问题。
- 修复 `BUG-F6-13`：Trojan 入站握手读取 CRLF 后现在会校验字节必须严格等于 `\r\n`，首段认证后和请求地址后的两处 CRLF 都复用同一校验逻辑，非法或截断握手会直接拒绝。
- 修复 `BUG-F6-14`：VLESS response addon discard 现在会检查并返回 `io.CopyN` 的错误，截断 addon 数据不再被静默吞掉，调用方能在响应头处理阶段得到协议读取错误。
- 修复 `BUG-A7-09`：Hysteria2 Realm inbound 的编译期接口断言改为 `(*Hysteria2RealmServer)(nil)`，让断言覆盖真正的目标类型。
- 新增回归测试覆盖 Trojan CRLF 校验和 VLESS 截断 response addon 错误返回；Hysteria2 Realm 接口断言由 `listener/inbound` 包编译测试覆盖。
- 验证：`rtk gofmt -w listener/trojan/server.go listener/trojan/server_test.go transport/vless/conn.go transport/vless/conn_test.go listener/inbound/hysteria2_realm.go` 已执行；`rtk go test ./listener/trojan ./transport/vless ./listener/inbound` 通过；`rtk git -c safe.directory=C:/Users/Sen/Documents/GitHub/mihomo diff --check -- listener/trojan/server.go listener/trojan/server_test.go transport/vless/conn.go transport/vless/conn_test.go listener/inbound/hysteria2_realm.go` 通过。

2026-06-05（proxy-group/listener 回归修复补充）

- 修复 `url-test` health-check 预热递归触发问题：`healthCheck()` 和显式 `URLTest()` 后的预热改为走 `fastWithHealthCheck(..., false)`，避免所有节点持续不可用时 `fast()` 再次异步触发 `healthCheck()` 形成后台循环。
- 修复 `url-test` / `fallback` 手动选择死节点回归：手动选择只有在目标节点仍 `AliveForTestUrl()` 时才会命中；`fallback` 会清理已失效的手动选择并切回自动选择，`url-test` 会跳过失效手动节点并选择可用节点。
- 修复手动选择状态误判：`DialContext` / `ListenPacketContext` 现在按“实际出站节点名是否等于 selected”判断 manual selected，自动 fallback 到其他节点后仍会正常重置 `fastSingle` 和触发失败统计。
- 修复 listener / DNS 同端口地址切换 reload 失败：新增同端口通配地址冲突判断，`127.0.0.1:port` 与 `:port`、`0.0.0.0:port` 等互相切换时会先释放旧 socket，再创建新监听；无端口冲突时仍保留“新实例创建成功后再替换旧实例”的安全路径。
- 恢复 Redir/TProxy UDP 启动失败语义：UDP 监听创建失败时只记录 warning，不再关闭已成功创建的 TCP listener，也不再把该错误汇总为 reload 失败。
- 新增回归测试覆盖手动选择死节点 fallback、`url-test` all-dead 预热不触发 health-check，以及 listener/DNS 同端口通配地址冲突判断。
- 提交并推送：`30012329 fix: address proxy group and listener regressions` 已推送到 `origin/Alpha`。
- 验证：`rtk go test ./adapter/outboundgroup` 通过；`rtk go test ./listener ./dns` 通过；`rtk go test ./...` 通过，175 个包共 4442 个测试通过；`rtk git diff --check` 通过。
